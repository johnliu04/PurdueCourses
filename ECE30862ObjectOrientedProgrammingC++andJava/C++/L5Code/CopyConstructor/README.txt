main.cpp shows how having a copy constructor usually isn't enough, we also need to overload the "=" operation.

main2.cpp shows that the copy constructor is what is needed to make copies when passing parameters.
