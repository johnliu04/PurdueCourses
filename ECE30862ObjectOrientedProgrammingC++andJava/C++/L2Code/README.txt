Access: code showing how the access modifiers (private, public and protected) work.

Duck: The code for the duck example.

Hiding: Code demonstrating hiding in C++.

NewDuck: Attempt to improve the Duck code.

NoPolyWIthVar: Shows that polymorphism doesn't work with object attributes (variables)

PolyNonVirtualBase: shows what happens when the base is not virtual

PolyWithNewFunc: Shows that when a derived class defines a new function that is not defined in the base class, and we try and call that new function through a base class pointer, an error results.
